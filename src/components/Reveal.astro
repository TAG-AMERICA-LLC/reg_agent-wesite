---
interface Props {
  delay?: number;
  as?: string;
  class?: string;
}
const { delay, as: As = 'div', class: className } = Astro.props;
---
<As data-reveal data-reveal-delay={delay} class={className}>
  <slot />
</As>
